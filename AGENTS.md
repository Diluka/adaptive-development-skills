# 仓库规则

## 任务范围

- Git 仓库是所有技能的唯一源码；不要直接编辑运行时技能安装目录。
- 核心目标是把需求具体化为工作单元，为每个单元选择当前最合适的主要开发方法，并贯穿实现、验证与交付生命周期。
- 只修改当前任务相关的技能与维护文件，保留无关改动。
- 是否创建开发分支按任务与用户要求决定；一旦创建或检出用于项目改动的开发分支且用户没有指定其他安排，默认在 `<repo>/.worktrees/<name>` 的独立工作树中进行。纯只读分支审查可在能安全保持或恢复干净默认基线时，由执行者选择使用根路径或工作树。
- lint 与格式化的只读检查可按项目要求覆盖必要范围；自动修复或格式化改写默认只作用于当前任务修改行，工具不支持时才扩大到最小必要语法单元或受影响文件，并审阅、移除非必要的无关差异。
- 文档目录优先遵循用户指定和项目现有规范。没有特别约定时，临时设计、计划、审查与评估产物放在被 Git 忽略的 `.docs/`，其中计划放在 `.docs/plans/`；用户或项目要求长期保留的正式项目文档放在 `docs/` 并纳入版本控制。
- 只有根代理可以派发子代理；子代理禁止继续派生。

## 开发任务判定

- 预期交付结果包含需要项目保留、集成、提交或交付的代码、配置、依赖、测试、脚本、技能、工作流或正式文档变更时，才是开发任务。
- 是否写代码、修改文件或只读不是判据。只交付结论和证据的调查或技术探索，即使编写临时原型、脚本、测试、插桩或临时安装依赖，也不是开发任务。
- 临时产物必须隔离且可丢弃，不得混入用户改动或最终项目差异；调查结束时检查依赖声明、锁文件、配置、生成产物和工作区状态。
- 调查中决定保留实验内容时，立即转换为开发任务：停止扩展临时代码，按实际增量重新定级，并明确保留、重写和删除范围后再进入正式实现。
- 一开始要求调查并完成实现的请求整体属于开发任务；只交付已经完成开发的提交、推送、PR/MR 或部署本身不是新的开发任务。
- 整体属于开发任务，不表示其中每个工作单元都使用开发方式。先按独立目标与证据边界把单元分为调查或开发；调查单元只交付事实、根因、可行性或决策输入，正式变更即使已获授权也要在调查结束后建立新的开发单元并重新判断适用方法。

## 任务分级与流程节点

- 先按当前请求造成的实际增量定级，不自动继承父任务、现有分支或整个 PR/MR 的复杂度。文件数和行数只是信号，运行行为、公共契约、影响范围、不确定性、可逆性、外部副作用和直接证据更重要。
- 从能保证正确性的最低充分级别开始；发现真实影响面后可以升降级。不要先套完整流程，再逐项论证为什么可以省略。
- **微小任务**目标明确、修改局部可逆、不改变运行行为或公共契约、不涉及依赖、生成规则、权限或产物自身的外部副作用，并能直接检查结果。直接实现、审阅差异、运行必要的直接检查并简要总结；不落盘计划，不派独立评审。
- **常规任务**包含边界明确的行为或配置变化，或者需要若干相关步骤，但没有高风险或重大不确定性。计划、实现委派和独立评审都按协调成本、证据收益和回归风险决定；使用覆盖受影响路径的定向验证。
- **复杂或高风险任务**涉及跨模块或公共契约、架构与依赖迁移、安全、身份、资金、数据、并发、破坏性或难回退操作、正式环境、显著未知，或者需要多代理共享状态。必须落盘计划并完成独立评审，按风险增加验证、专项评审和回退安排。
- 对每个计划、确认、委派、测试、评审或交付节点，都要能说明它会发现当前任务引入的哪种合理错误；现有证据已经覆盖或节点没有实际收益时跳过。用户要求的交付方式是强信号，但不能覆盖真实风险或扩大未授予的提交、推送、PR/MR、合并与部署权限。已明确授权的普通 Git 交付动作单独核对权限和执行结果，不因修改远端引用而提升内容任务级别；其他外部状态变化仍按真实风险定级。

## 计划与协作

- 微小任务不创建计划文件；常规任务只在多步骤协调、需要恢复上下文或计划本身能降低错误风险时落盘；复杂或高风险任务必须落盘计划。
- 计划至少记录目标与预期结果、范围、工作单元、各单元主要方法、验证方式，以及独立评审是否适用及其理由。复杂或高风险任务再补充依赖顺序、关键假设、决策点、风险、回退方式、分工、评审深度、专项评审者与交付安排。
- 原始请求已经明确授权目标行为和范围、没有未决取舍、风险受控且操作在现有授权内时，计划只是执行依据，不再要求第二次批准。只有存在未解决的业务取舍或关键歧义、拟偏离已授权行为或范围、高风险或破坏性操作、正式环境、非普通 Git 交付的外部写入、权限扩大或新增副作用时，才在实施前确认计划。
- 新事实使调整仍保持原目标、范围、业务行为、交付结果和授权不变，且没有新增副作用时，更新计划并告知用户后继续；否则暂停并请求确认。
- 需要计划时，一份计划覆盖整个开发任务，不要求每个工作单元单独建文件。多代理协同时，计划是统一对齐文档；根代理维护目标、分工、依赖、共享状态、决策和进展，子代理读取相关单元并回报证据，不并发改写计划。
- 计划是可修正的工作依据，不能凌驾于需求、不变量、真实调用方式、依赖契约和新证据。
- 计划创建、更新、状态同步和处理评审意见属于原开发任务，不递归产生新的任务或计划。

## 子代理与独立评审

- 边界清晰且能显著节约根代理局部上下文，或独立视角能减少实现者自证偏差时，优先委派调查、实现、证据设计或定向验证；小改动、共享状态紧密或交接成本更高时由根代理直接完成。
- 微小任务不派最终独立评审；常规任务只在独立视角能发现定向验证覆盖不到的合理错误时评审；复杂或高风险任务的最终项目差异必须由未参与实现、证据设计或相关差异修改的独立子代理至少评审一轮。
- 评审者读取需求、适用计划、完整实际差异和最新验证证据，原则上只读，按严重程度报告带文件和行号的意见，不直接修改被审查文件，也不得继续派生。
- 主代理自查、已有适用评审、PR/MR 评审、CI 和运行时验证各自可以提供证据，但不能互相冒充。选择独立评审时说明它补充的风险面，不为满足形式重复已有证据。
- 评审后只有影响既有结论或风险面的实质修改才使对应评审失效；重新验证和复审受影响范围，不从头重复无关部分。无法创建评审子代理时，只阻止本来就要求独立评审的任务声明完成。
- 非开发任务和无需独立评审的开发任务不强制评审；复杂功能调研可在明显提高目标覆盖和结论准确性时选择评审代理。

## 事实与方法

- 开发方式、调查方法、集成与交付方法、生命周期支持相互正交。每个调查或开发工作单元只选择一个主要方法；SDD、BDD 等上游方法产生的实现目标继续拆成后续工作单元。集成与交付及生命周期支持按需叠加，不参与主要方法竞争。
- 标准开发方式一旦选中，保持其行业语义和核心流程：TDD 使用 Red → Green → Refactor；SDD 贯通 Requirements、Design / Plan、Tasks 与 Implement；BDD 使用 Discovery、Formulation、Automation；Type-Driven Design 从稳定不变量、可信边界和类型建模推进到静态与剩余运行时证据；属性测试、使用方驱动契约测试、特征测试和评估驱动开发各自保留独立闭环。仓库选择门禁和安全约束只能包围方法，不能重新定义方法。
- 源码研究、架构审查、契约核验、系统化调试、技术探索和探索性测试都是调查方法，只交付可追溯证据。调查产物被接收后，任何长期测试、契约、文档或实现都属于新的开发单元。
- 主干开发、持续交付和渐进式交付属于集成与交付层，分别处理频繁进入主干、持续保持可发布和正式环境受控暴露；它们不能替代功能开发期证据，也不自动扩大推送、合并、发布、部署或正式环境写入权限。
- 需求、明确不变量、真实生产调用方与依赖契约高于测试和流程清单。
- 测试与实现冲突时同时审查两者，修改违反真实需求的一方。
- 不得只为人工测试输入添加生产代码选项、分支、包装层、回退或抽象；出现这种需要时停止实现并重新审查测试。
- 模拟对象只用于时钟、网络、进程等真实外部边界。
- 可测试不等于适合 TDD。TDD 只用于具有独立、稳定判据、真实测试接缝和具体回归风险的确定性行为变更；无法说明额外证据收益时直接实现。
- 有静态类型不等于适合 Type-Driven Design。只有稳定领域不变量能被类型构造显著编码、类型建模构成独立目标并能排除合理错误实现时才选用；普通字段或类型标注直接实现，不可信外部输入仍需运行时解析，类型系统无法证明的算法、副作用、并发、性能和概率性风险保留对应运行时证据。
- 不得在测试中复制提示词、文案、静态常量或实现细节制造镜像断言；精确内容本身属于协议、安全、合规或其他外部契约时除外。
- 快照必须来自真实执行路径中全部或部分可稳定复现的业务输出，用于行为基线或回归验证；有意更新快照不自动属于 TDD，也不得无条件接受新快照。
- 主观、概率性或存在多种合理答案的结果使用代表性样例、评价标准、实验、冒烟或独立评估；只把已经确认的稳定不变量转成长期自动化测试。
- 已有测试、评审、观测或 CI 只在当前变化影响其结论时失效；复用仍覆盖相同风险的证据，只补受影响范围。分支集成和冲突解决优先检查实际重叠与最终差异，并把新提交上的 CI 作为组合结果的一等证据。
- 技能和代理工作流属于启发式规则时，结构检查只能证明格式与引用有效，子代理模拟也可能受已安装技能和会话上下文干扰。把模拟结果视为有限评估，通过真实任务实践观察效果，并根据具体反馈继续迭代。
- 开发或审查技能期间不得写入、修改、删除或迁移正式环境数据源。

## 技能编写

- 每个技能放在 `skills/<skill-name>/SKILL.md`；目录与 YAML 前置元数据中的 `name` 必须是匹配的英文短横线命名。
- 技能目录下的 `resources/` 存放该技能配套的参考文档（纯 Markdown，不带技能前置元数据），随技能作为最小安装单元安装，由技能正文链接按需读取。
- `description` 以 `Use when` 开头，使用中英混合文字描述触发条件，不在其中概述流程。
- `SKILL.md` 规则正文以中文为主。有稳定中文译名的技术概念直接使用中文，不在各技能中重复括注英文；中英对照和英文保留边界统一维护在 `skills/adaptive-development-workflow/resources/terminology.md`。只有为保持字面精确所必需的 API、SDK、CLI、TDD、Git 命令、路径、符号、技能名和 PR/MR 等技术标识保留英文。
- 优先写简洁决策规则。计划、验证、独立评审和交付节点按任务级别与实际证据收益决定；用于项目改动的开发分支在没有额外要求时仍默认绑定独立工作树。是否创建分支、只读访问位置、用户审批、TDD、实现子代理、PR/MR 和提交按工作单元、风险与授权决定。
- 规则正文以正面叙述为主。反面表述只用于必须禁止的事项；不写自我指涉的元叙述（如“不再赘述”），也不写与正面表述重复的否定句。
- `adaptive-development-workflow` 负责共享入口的分类、拆分、选法与转换；用户明确调用标准方法时，由对应技能自行核验触发条件和适用前提，不要求共享入口预选。标准方法技能各自说明主要流程、排除场景、证据产物及相邻边界。`evidence-management` 只维护证据策略、复用和最终组合收敛，不作为其他方法的必经预选或收尾门，也不复制各标准方法正文。
- 不创建宽泛的静态验证技能。静态编译与分析由 `evidence-management` 作为证据管理；只有未来出现真实模型检查或证明需求时，才重新评估边界明确的形式化验证技能。
- 本仓库只收录通用开发方法，不增加生态、框架、产品或具体业务领域的操作手册。
- PR/MR 可作为通用 Git 工作流术语，但不要加入某个托管平台的专用 CLI、API 或字段说明。
- 创建或修改技能后，使用当前 `skill-creator` 技能内置的 `scripts/quick_validate.py` 逐个校验；不要在本仓库重复实现基础格式校验。
- 本地技能依赖使用相对 Markdown 链接，并通过 `scripts/check-cross-references.ts` 检查技能间引用。
- 只在 `README.md` 说明思想来源；本仓库不跟踪、不同步上游版本，也不承担上游兼容义务。

## Plugin 与 Hook 发行

- `skills/` 是唯一 Skill 源码，同时服务于 standalone `npx skills` 与可选 Codex Plugin；不得为 Plugin 复制、搬迁或生成第二份 Skill。
- Skill 与 Hook 必须解耦。`skills/*` 不得引用、依赖或要求安装 Hook；新增或修改 Hook 时也不得把具体 Skill 的方法、规则或触发逻辑写入 Hook。
- Hook 只作为可选 Plugin 能力分发。standalone `npx skills` 的安装、更新和运行不得依赖 Plugin manifest、marketplace 或 Hook 状态。
- 修改 Plugin 或 Hook 时，核对 marketplace 仍为显式可选安装、Plugin Hook 仍需独立审阅信任，并验证未把 Hook 带入 standalone Skill 安装结果。
- Hook 的可写状态只能放在 launcher 从系统临时目录变量拼接出的 Plugin 专属子目录，并通过 `TASK_HANDOFF_DATA` 传给脚本；Deno 只获得该子目录的读写权限。脚本通过 `PLUGIN_ROOT` 定位，不写目标项目临时目录，不硬编码安装路径，不解析未承诺稳定的 transcript 内部格式。通过获准子进程读取项目元数据前，必须核验命令不会触发 Git hook、filter、helper 或其他外部进程；Deno 的 `--allow-run` 不会继续沙箱化子进程。
- Hook 生产脚本只使用 Deno 2 与 Node 24 共同支持的 Node 内置 API；launcher 优先使用 Deno，只有找不到 `deno` 时才回退 Node，选中的运行时失败后不得再次执行另一运行时。Node fallback 的子进程、环境变量和网络权限比 Deno 更宽，修改入口或权限时必须同步审阅两条路径。Windows 的 `commandWindows` 不假设 Codex 使用哪种会话 shell，必须显式通过 `cmd.exe` 执行 `.cmd` launcher。
- `hooks/codex-hook-types.ts` 按当前最低核对 Codex 版本的官方生成 schema 约束 stdin 与逐事件 stdout；修改事件、字段或支持版本时先更新该类型边界，并以 `deno check` 作为接口静态检查。执行测试只验证脚本自身设计逻辑，每个已配置 Hook 保留一个代表性测试，不用人工事件镜像重复类型约束，也不把独立脚本测试称为 Codex 集成测试。
- 正式发布 Plugin 变更时递增 manifest 版本；Git marketplace 用户通过 `codex plugin marketplace upgrade` 获取新快照。本地开发迭代按当前 `plugin-creator` 的 cachebuster 与重装流程验证，不把同版本缓存覆盖当作发行契约。

## 完成检查

微小文档改动只运行能直接证明结果的检查和 `git diff --check`。创建或修改单个技能时，用当前 `skill-creator` 校验受影响技能；修改共享入口、生命周期规则或技能间引用时运行以下完整检查：

```bash
skill_creator_dir="${CODEX_HOME:-$HOME/.codex}/skills/.system/skill-creator"
for skill_dir in skills/*; do
  python3 "$skill_creator_dir/scripts/quick_validate.py" "$skill_dir" || exit 1
done
deno check scripts/check-cross-references.ts
scripts/check-cross-references.ts
git diff --check
```

修改 Plugin 或 Hook 时，另运行：

```bash
deno fmt --check hooks/codex-hook-types.ts hooks/task-handoff.ts hooks/tests/task-handoff.test.ts
deno lint --no-config hooks/codex-hook-types.ts hooks/task-handoff.ts hooks/tests/task-handoff.test.ts
deno check --no-config --no-lock --no-npm --no-remote hooks/codex-hook-types.ts hooks/task-handoff.ts
node --check hooks/codex-hook-types.ts && node --check hooks/task-handoff.ts
sh -n hooks/run-task-handoff.sh
test -x hooks/run-task-handoff.sh
deno test --no-config --no-lock --no-npm --no-remote --allow-read --allow-write --allow-run=deno hooks/tests/task-handoff.test.ts
python3 "${CODEX_HOME:-$HOME/.codex}/skills/.system/plugin-creator/scripts/validate_plugin.py" .
```

Hook CI 固定使用 Deno 2.9.4、Node 24.15.0 和 Codex CLI 0.146.1。Ubuntu 运行上述静态检查、现有四个 Hook 测试、真实 Deno/Node launcher 冒烟和一次临时 `CODEX_HOME` Plugin 安装校验；Windows 同时从 PowerShell 外层和 `cmd.exe` 外层执行 `.cmd`，覆盖 Deno 优先、Node fallback、stdin/stdout、检查点以及 `.cmd` 退出码透传。它们是脚本与 Plugin 的运行时冒烟，不是 Codex 压缩集成测试。
