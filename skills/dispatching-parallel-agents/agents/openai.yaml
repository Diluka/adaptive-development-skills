interface:
  display_name: "Dispatching Parallel Agents"
  short_description: "为吞吐或独立对照并行可安全隔离且不共享可变状态的工作单元"
  default_prompt: "使用 $dispatching-parallel-agents 判断并行吞吐或独立对照是否确有收益；只派发可安全隔离的单元，也允许选择顺序协作。"
